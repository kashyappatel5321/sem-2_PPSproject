#include<stdio.h>
#include<conio.h>
int main(){
    float r;
    float a=22/7,b;
    printf("enter radiusof circle:");
    scanf("%f",&r);
    printf("yuor circle's area is %f",b=a*r*r);
    getch();
    return 0;
}
